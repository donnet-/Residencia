<div style="margin-left: 35px;">
    <%= form_tag(docentes_actividad_path, method: 'get') do %>
        <div class="row">
            <%= hidden_field_tag :format, "pdf"%>
            <%= hidden_field_tag :actividad_id, @actividad.id %>
            <%#= submit_tag "Generar Lista", :formtarget => '_blank', class: 'btn btn-success' %>
			<%= submit_tag "Ver lista", :formtarget => '_blank', class: 'btn btn-success' %>
        </div>
    <% end %>
</div>