<div align="right">
    <%= form_tag(control_actividad_investigaciones_actividades_path, method: 'get') do %>
        <div class="row">
            <%= hidden_field_tag :format, "pdf"%>
			
            <%= hidden_field_tag :actividades_inv, @actividad_investigaciones.collect(&:id) %>
            <%= submit_tag "Generar Lista", :formtarget => '_blank', class: 'btn btn-success' %>
        </div>
    <% end %>
</div>