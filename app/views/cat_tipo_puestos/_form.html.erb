<div class="container">
    <div id="align-forms">
        <%= nested_form_for @tipoPuesto, validate: true do |tipoPuesto| %>
            <div class="row">
                <%= tipoPuesto.label :Nombre_del_Puesto, :class => 'col-md-3' %>
                <div class="col-md-4">
                    <%= tipoPuesto.text_field :nombrePuesto, :class => 'form-control', :style => 'border: 1px solid red', validate: true %>
                </div>
            </div>
        
            <div class="row">
                <%= tipoPuesto.label :Género_puesto, :class => 'col-md-3' %>
                <div class="col-md-4">
                <%= tipoPuesto.text_field :alterno, :class => 'form-control', :style => 'border: 1px solid red', validate: true %>
                </div>
            </div>
        
            <div class="row">
                <%= tipoPuesto.label :Estado, :class => 'col-md-3' %>
                <div class="col-md-4">
                    <%= tipoPuesto.select :estado, options_for_select([["Activo", "Activo"], ["Inactivo", "Inactivo"]]),{}, :class => 'form-control', validate: true %>
                </div>
            </div>
            
            <div class="row">
                <div class="col-xs-2 col-md-4" id="btn-cancelar">
                    <%= tipoPuesto.submit :Guardar, :class => 'btn btn-success' %>
                </div>   
        <% end %>
                <div class="col-xs-2 col-md-4" id="btn-cancelar">
                        <%= button_to "Cancelar", cat_tipo_puestos_path, :class => 'btn btn-warning', :method => :get %>
                </div>
            </div>
    </div>
</div>