<div class="container">
    <%= render template: "siged/menuDocente" %>

    <h3 id="align-title">Cátalogo de Criterios para la evaluacion del Servicio Social</h3>
    
    <div class="row">
        <div class="col-xs-6 col-sm-1">
            <% if can? :create, CatCriterioEvalServicio %>
                <%= button_to new_cat_criterio_eval_servicio_path, :class => "btn btn-primary", :method => :get do %>
                     <span class="glyphicon glyphicon-plus"></span>
                <% end %>
            <% end %>
        </div>

        <div class="col-xs-6 col-sm-9">
            <table class="table table-bordered">
                <thead>
                    <th class="info"><%= sortable "Nombre del criterio" %></th>
                </thead>
                <% @cat_criterio_eval_servicios.each do |cat_criterio_eval_servicio| %>
                    <tbody>
                        <td class="active"><%= cat_criterio_eval_servicio.nombre_criterio %></td>
                
                        
                        <td class="active">
                            <%= link_to edit_cat_criterio_eval_servicio_path(cat_criterio_eval_servicio) do %>
                                <span class="glyphicon glyphicon-pencil green"></span>
                                <div class="tip" id="action2">Editar</div>
                               
                            <% end %>

                        </td>
                    </tbody>
                <% end %>
            </table>
           
        </div>
    </div>
</div>