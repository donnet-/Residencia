
    <% if current_usuario.rol == 'admin' %>
        <%= render template: "siged/menuAdmin" %>
    <% elsif current_usuario.rol == 'docente' %>
        <%= render template: "siged/menuDocente" %>
    <% end %>
    <% breadcrumb :criterio_academia_edit, @academia %>
<div class="container">
    <h1 id="align-title">Editar registro para el Cátalogo de Criterios de Academia</h1>
    
    <%= render 'form' %>
</div>