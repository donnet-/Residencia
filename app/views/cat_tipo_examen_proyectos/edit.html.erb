<div class="container">
    
    <% if current_usuario.rol == 'admin' %>
        <%= render template: "siged/menuAdmin" %>
    <% elsif current_usuario.rol == 'docente' %>
        <%= render template: "siged/menuDocente" %>
    <% end %>
    <% breadcrumb :cat_tipo_examen_proyecto_edit, @tipoexproy%>

    <h3 id="align-title">Editar registro del Catálogo de Tipo de Examen Pofesional y Proyectos</h3>
    <%= render 'form' %>
</div>