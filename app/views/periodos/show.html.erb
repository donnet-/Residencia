<div class="container">
    <% if current_usuario.rol == 'admin' %>
        <%= render template: "siged/menuAdmin" %>
    <% elsif current_usuario.rol == 'docente' %>
        <%= render template: "siged/menuDocente" %>
    <% end %>
</div>
<% breadcrumb :periodo, @periodo %>
<div class="container"> 
    <h2 align="center">Ver periodo</h2> 
    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
        <p> <strong>Periodo:</strong> <%= @periodo.periodo %></p> 
        <p> <strong>Fecha de Inicio:</strong> <%= @periodo.fechaInicio.strftime('%d/%m/%Y') %></p> 
        <p> <strong>Fecha de Término:</strong> <%= @periodo.fechaTermino.strftime('%d/%m/%Y') %></p>            
        <br>
        <%= link_to "Inicio", periodos_path %>
    </div>
    </div> 
</div>