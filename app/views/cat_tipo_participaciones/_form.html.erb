<div class="contenedor">
    <div id="align-forms">
        <%= nested_form_for @tipoParticipacion, validate: true do |tipoParticipacion| %>
            <div class="row">
                <%= tipoParticipacion.label :Tipo_participación, :class => 'col-md-3' %>
                <div class="col-md-4">
                    <%= tipoParticipacion.text_field :participacion, :class => 'form-control', :style => 'border: 1px solid red', validate: true %>
                </div>
            </div>
            
            <div class="row">
                <%= tipoParticipacion.label :Estado, :class => 'col-md-3' %>
                <div class="col-md-4">
                <%= tipoParticipacion.select :estado, options_for_select([["Activo", "Activo"], ["Inactivo", "Inactivo"]]),{}, :class => 'form-control', validate: true %>
                </div>
            </div>
        
            <div class="row">
                <div class="col-xs-2 col-md-4" id="btn-cancelar">
                    <%= tipoParticipacion.submit :Guardar, :class => 'btn btn-success' %>
                </div>   
        <% end %>
                <div class="col-xs-2 col-md-4" id="btn-cancelar">
                        <%= button_to "Cancelar", cat_tipo_participaciones_path, :class => 'btn btn-warning', :method => :get %>
                </div>
            </div>
    </div>
</div>