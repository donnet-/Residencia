<div>
	<% if current_usuario.rol == 'admin' %>
        <%= render template: "siged/menuAdmin" %>
    <% elsif current_usuario.rol == 'docente' %>
        <%= render template: "siged/menuDocente" %>
    <% end %>
</div>
<div class="container">
    <% breadcrumb :cat_especialidad_new%>

    <h3 id="align-title">Registro para el Cátalogo de Carreras</h3>
    <%= render 'form' %>
</div>