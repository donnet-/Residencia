<div class="container">
    
    <% if current_usuario.rol == 'admin' %>
        <%= render template: "siged/menuAdmin" %>
    <% elsif current_usuario.rol == 'docente' %>
        <%= render template: "siged/menuDocente" %>
    <% end %>
    <%# breadcrumb :cat_evidencia_new %>

    <h3 id="align-title">Registro al Cátalogo de Evidencias para Actividades</h3>
    <%= render 'form' %>
</div>